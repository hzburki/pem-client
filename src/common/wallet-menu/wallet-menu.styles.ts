import { createStyles } from "@mantine/core";

export const walletMenuButtonStyles = createStyles((theme) => ({
  root: {
    border: 0,
    maxHeight: 40,
    marginLeft: 20,
    borderWidth: 0,
    display: 'block',
    borderColor: "transparent",
    backgroundColor: 'transparent',
  }
}));